this is a software with react frontend and spring backend to handle robust thousands of users simultaneously. this software is digital solution for tradition chit funds. there are two models this software tries to cover 1) collect a fixed amount from every member of group every month. this fund will be then managed by the president/ leader of group. 2) all member collect fix amount a auction will occur and he will win all the amount, then next month same like that.
in the software enable loan approvement, voting for various resons admin can arrange as traditionaly they take decision as per majority. a loan taker can see how much loan he has from each group as he can be part of more than one group. he should be able to see his loan in real time means each day not a per minute or per hr but  daily change.
a admin of group can see, how much loan each member has for how much period each member has been alloted to retun the money.
there will be a poster area for each group where admin can post photos videos. admin also has access to remove add members
software should manage the loans its interest from each member to each group. automatically calculate and loan interest each members saving with separate principle and savings profit he received from the interest collceted by group as it will be divided amongs all group members evenly at fixed period decided i.e 2 years etc.
there will be a chatting area for each group privately only members of that group can participate and message into that we have to implement this as not considered earlier.
each user has role such as member, admin accoutant
user status, group status. automatic notifications should be sent on various occasions, notifications should be configurable by admin. i.e for each month money collection date is 15 then message 2 day early it can be changed by admin add such functionality for loan return loan period etc.
other functionality is not implemented but some classes are defined which named based on functions
for calculation and easy understanding of each user of group admin can generated reports etc the functionality we have to implement so each can understand what happned in group.
we will implement its frontend  chit-client later.
add all the methods and classes with metadata and documentation for a documentation generation in html/ not only javadoc but a real application documentation. we will add this section on website
if you can do both simultaneously the ok keep frontend theme configurable.
change and do each functionality with commit dont implements lot of file then commit implement a finctionality make a human written small precise simple  commit to it then move next
i have excluded the chit-client from this backend git repositoy but chit-client is a git repo so made separate commit for both as i told commits should be human written simple small precisse understandable.

feel free to change and suggest new functionality to add in this repo.